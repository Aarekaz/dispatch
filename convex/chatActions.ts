"use node";

/**
 * Chat actions — Node.js runtime.
 *
 * Currently empty. Reserved for future use:
 * - Vercel Workflow callbacks for background/channel tasks
 * - Batch message persistence from webhook handlers
 *
 * The interactive chat flow uses the Next.js API route (Vercel compute)
 * and persists completed messages via mutations in chat.ts.
 */
